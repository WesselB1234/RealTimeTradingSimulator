@{
    @using static RealTimeStockSimulator.Models.Helpers.StringFormatter
    @using static RealTimeStockSimulator.Models.Enums.NavEnabled;
}

<nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light border-bottom box-shadow mb-3 primary-background-color">
    <div class="container-fluid">
        <span class="navbar-brand text-light">Real-Time Trading Simulator</span>
        <ul class="navbar-nav flex-grow-1">
            <li class="nav-item">
                <a class="nav-link nav-hover text-light @(ViewBag.NavEnabled == Portfolio ? "nav-pill-enabled" : "")" href="/Portfolio/Index">Portfolio</a>
            </li>
            <li class="nav-item">
                <a class="nav-link nav-hover text-light @(ViewBag.NavEnabled == Assets ? "nav-pill-enabled" : "")" href="/Assets/Index">Assets</a>
            </li>
            <li class="nav-item">
                <a class="nav-link nav-hover text-light @(ViewBag.NavEnabled == Leaderboard ? "nav-pill-enabled" : "")" href="/Users/Leaderboard">Leaderboard</a>
            </li>
            <li class="nav-item">
                <a class="nav-link nav-hover text-light @(ViewBag.NavEnabled == Transactions ? "nav-pill-enabled" : "")" href="/Transactions/Index">Transactions</a>
            </li>
            <li class="nav-item">
                <a class="nav-link nav-hover text-light @(ViewBag.NavEnabled == Settings ? "nav-pill-enabled" : "")" href="/Settings/Index">Settings</a>
            </li>
            <li class="nav-item ms-lg-auto">
                <span class="nav-link text-light">               
                    Balance: $@FormatDecimalPrice(ViewBag.LoggedInUser.Money)
                </span>
            </li>
            <li class="nav-item">
                <span class="nav-link text-light">
                    Logged in as @ViewBag.LoggedInUser.UserName
                </span>
            </li>
            <li class="nav-item">
                <a class="btn btn-danger" href="/Authentication/Logout">Logout</a>
            </li>
        </ul>
    </div>
</nav>