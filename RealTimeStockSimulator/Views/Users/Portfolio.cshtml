@{
    @model RealTimeStockSimulator.Models.ViewModels.UserPortfolioVM
    @using static RealTimeStockSimulator.Models.Helpers.StringFormatter
    @using System.Text.Json
    @using RealTimeStockSimulator.Extensions

    ViewData["title"] = $"{Model.User.UserName}'s Portfolio";
}

<partial name="_Navbar" />

<main class="w-75 mx-auto">
    <h1>
        @Model.User.UserName's Portfolio
    </h1>
    <h2>
        Total value: <span data-price="@FormatDecimalToJsDecimal(Model.Tradables.GetTotalOwnershipValue())" class="TotalOwnershipValue">$@FormatDecimalPrice(@Model.Tradables.GetTotalOwnershipValue())</span>
    </h2>

    <partial name="_ErrorMessage" />
    <partial name="_ConfirmMessage" />

    <table class="table mt-4">
        <thead class="table-dark">
            <tr>
                <th>
                    Asset
                </th>
                <th>
                    Symbol
                </th>
                <th>
                    Type
                </th>
                <th>
                    Value
                </th>
                <th>
                    Amount owned
                </th>
                <th>
                    Total value
                </th>
                <th>
                    Actions
                </th>
            </tr>
        </thead>
        <tbody>
            @{
                foreach (OwnershipAsset tradable in Model.Tradables)
                {
                    <tr>
                        <partial name="_AssetTdCosmetics" model=tradable />
                        <td>
                            @tradable.Symbol
                        </td>
                        <td>
                            @tradable.Type
                        </td>
                        <td data-price-symbol="@tradable.Symbol" data-price="@FormatDecimalToJsDecimal(tradable.TradablePriceInfos.Price)">
                            $@FormatDecimalPrice(tradable.TradablePriceInfos.Price)
                        </td>
                        <td data-amount-label-number="1" data-amount-label-symbol="@tradable.Symbol" data-amount-label-value="@tradable.Amount">
                            @tradable.Amount
                        </td>
                        <td data-price-symbol="@tradable.Symbol" data-price="@FormatDecimalToJsDecimal(tradable.TradablePriceInfos.Price)" data-amount-label-number="1">
                            $@FormatDecimalPrice(tradable.TotalValue)
                        </td>
                        <td>
                            <a class="btn primary-custom-btn" href="/Assets/Buy?Symbol=@tradable.Symbol">Buy</a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</main>

<partial name="_Footer" />

<script>
    let ownershipJson = @Html.Raw(JsonSerializer.Serialize(Model.Tradables));
</script>
<partial name="_MarketConnectionScripts" />